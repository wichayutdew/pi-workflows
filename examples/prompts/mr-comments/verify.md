Review the implementation against the requested merge-request feedback and approved plan.

Inspect the diff and run the configured checks. Complete with `passed` only when evidence supports completion. Complete with `failed` to return to implementation, or `blocked` when a human must repair the workflow or environment.
