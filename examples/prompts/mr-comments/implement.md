Implement the approved plan with a small, coherent diff.

Use repository conventions and the allowed skill. Add or update focused tests where practical. Do not perform remote actions. Complete with `ready` when implementation and focused checks are complete. Complete with `blocked` if the harness, configuration, or repository must be fixed first.
